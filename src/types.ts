export interface Launch {
  id: string;
  success: boolean;
  details: string;
  name: string;
  date: string;
}

export interface LaunchInfo {
  success: boolean;
  details: string;
  name: string;
  date: string;
}
